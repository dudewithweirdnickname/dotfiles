#!/usr/bin/sh
#
# guess i made it by myself and for myself to search through
# dog pile of shitbooks in dog pile of subdirectories
# should optimize to shit more effective via luke's recipe of
# testing time on scripts
#
#
#
#

#realpath $(
ls -R | grep --color=auto -i $1 
#)
 
#realpath $(ls -R)  | grep --color=auto -i $1 
